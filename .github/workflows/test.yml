name: Test Suite

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]
  workflow_dispatch:  # Allow manual trigger

jobs:
  test:
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        node-version: [18.x, 20.x]
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'
        
    - name: Install dependencies
      run: npm ci
      
    - name: Run linter
      run: npm run lint
      
    - name: Run basic unit tests (Jest)
      run: |
        echo "🧪 Running basic unit tests..."
        npm test -- test/basic.test.js
        
    - name: Run core component tests (Jest)
      run: |
        echo "🔧 Running core component tests..."
        npm run test:core
        
    - name: Run DHT algorithm concept test
      run: |
        echo "🔬 Running DHT algorithm concept test..."
        npm test -- test/local/basic-dht-concept.test.js
        
    - name: Run simple network simulation test
      run: |
        echo "🌐 Running simple network simulation..."
        npm test -- test/local/simple-network.test.js
      
    # Coverage reporting disabled for now
    # - name: Upload coverage to Codecov
    #   if: matrix.node-version == '20.x'
    #   uses: codecov/codecov-action@v3
    #   with:
    #     token: ${{ secrets.CODECOV_TOKEN }}
    #     files: ./coverage/lcov.info
    #     fail_ci_if_error: false
    #     verbose: true
        
    - name: Build project
      run: npm run build
      
    - name: Archive test results
      if: failure()
      uses: actions/upload-artifact@v4
      with:
        name: test-results-${{ matrix.node-version }}
        path: |
          coverage/
          test-results.xml
        retention-days: 7

  security:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20.x'
        cache: 'npm'
        
    - name: Install dependencies
      run: npm ci
      
    - name: Run security audit
      run: npm audit --audit-level high || true
      
    - name: Check for high/critical vulnerabilities only
      run: npm audit --audit-level high